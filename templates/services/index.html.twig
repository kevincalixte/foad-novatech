{% extends 'base.html.twig' %}

{% block title %}{{page.title}}{% endblock %}

{% block body %}
<div class="flex flex-col items-center justify-center min-h-[60vh]">
      <h1 class="mb-6">{{page.subject}}</h1>
      <ul class="space-y-2">
            {% for service in services %}
                  <li>{{ service }}</li>
            {% endfor %}
      </ul>
</div>
{% endblock %}